import { Decimal as DecimalJs } from "decimal.js";
export declare namespace Number {
    const toString: (num: number) => string;
}
export declare namespace String {
    const toNumber: (str: string) => number;
    const toBigInt: (str: string) => bigint;
    const toDecimal: (str: string) => DecimalJs;
}
export declare namespace HexString {
    const toUint8Array: (str: string) => globalThis.Uint8Array;
}
export declare namespace Uint8Array {
    const from: (arg: string | globalThis.Uint8Array | null | undefined) => globalThis.Uint8Array;
    const toHexString: (array: globalThis.Uint8Array) => string;
}
export declare namespace BigInt {
    const toString: (num: bigint) => string;
}
export declare namespace Decimal {
    const toString: (decimal: DecimalJs) => string;
}
