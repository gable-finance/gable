import { PublicKey } from "..";
export declare class DeriveBabylonAddressFromOlympiaAddressInput {
    networkId: number;
    olympiaAccountAddress: string;
    constructor(networkId: number, olympiaAccountAddress: string);
    toString(): string;
    toObject(): Record<string, any>;
}
export declare class DeriveBabylonAddressFromOlympiaAddressOutput {
    babylonAccountAddress: string;
    publicKey: PublicKey.PublicKey;
    constructor(babylonAccountAddress: string, publicKey: PublicKey.PublicKey);
    toString(): string;
    toObject(): Record<string, any>;
}
