import { EntityType } from "../..";
export declare class DecodeAddressInput {
    address: string;
    constructor(address: string);
}
export declare class DecodeAddressOutput {
    networkId: number;
    networkName: string;
    entityType: EntityType;
    data: Uint8Array;
    constructor(networkId: number, networkName: string, entityType: EntityType, data: Uint8Array | string);
    toString(): string;
    toObject(): Record<string, any>;
}
