import { PublicKey } from "../crypto";
export declare class DeriveVirtualIdentityAddressInput {
    networkId: number;
    publicKey: PublicKey.PublicKey;
    constructor(networkId: number, publicKey: PublicKey.PublicKey);
    toString(): string;
    toObject(): Record<string, any>;
}
export declare class DeriveVirtualIdentityAddressOutput {
    virtualIdentityAddress: string;
    constructor(virtualIdentityAddress: string);
    toString(): string;
    toObject(): Record<string, any>;
}
