import { SborValue } from "models/value";
export type SborEncodeInput = SborValue.Value;
export declare class SborEncodeOutput {
    encodedValue: Uint8Array;
    constructor(encodedValue: Uint8Array);
    toString(): string;
    toObject(): Record<string, any>;
}
