import { PublicKey } from "../crypto";
export declare class DeriveVirtualAccountAddressInput {
    networkId: number;
    publicKey: PublicKey.PublicKey;
    constructor(networkId: number, publicKey: PublicKey.PublicKey);
    toString(): string;
    toObject(): Record<string, any>;
}
export declare class DeriveVirtualAccountAddressOutput {
    virtualAccountAddress: string;
    constructor(virtualAccountAddress: string);
    toString(): string;
    toObject(): Record<string, any>;
}
