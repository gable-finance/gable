export declare class EncodeAddressInput {
    addressBytes: Uint8Array;
    networkId: number;
    constructor(addressBytes: Uint8Array | string, networkId: number);
    toString(): string;
    toObject(): Record<string, any>;
}
export declare class EncodeAddressOutput {
    address: string;
    constructor(address: string);
    toString(): string;
    toObject(): Record<string, any>;
}
