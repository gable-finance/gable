import { Instruction } from ".";
export declare class InstructionList {
    readonly type: Kind;
    constructor(type: Kind);
}
export declare enum Kind {
    String = "String",
    Parsed = "Parsed"
}
export declare class StringInstructions extends InstructionList {
    value: string;
    constructor(instructions: string);
    toString(): string;
    toObject(): Record<string, any>;
}
export declare class ParsedInstructions extends InstructionList {
    value: Array<Instruction.Instruction>;
    constructor(instructions: Array<Instruction.Instruction>);
    toString(): string;
    toObject(): Record<string, any>;
}
