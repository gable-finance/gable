import { InstructionList, TransactionIntent } from ".";
import { SignatureWithPublicKey } from "../../models/crypto";
export declare class SignedTransactionIntent {
    intent: TransactionIntent;
    intentSignatures: Array<SignatureWithPublicKey.SignatureWithPublicKey>;
    constructor(intent: TransactionIntent, intentSignatures: Array<SignatureWithPublicKey.SignatureWithPublicKey>);
    compile(): Promise<Uint8Array>;
    static decompile(compiledIntent: Uint8Array, instructionsOutputKind?: InstructionList.Kind): Promise<SignedTransactionIntent>;
    transactionId(): Promise<Uint8Array>;
    signedIntentHash(): Promise<Uint8Array>;
    toString(): string;
    toObject(): Record<string, any>;
}
