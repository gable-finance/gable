import { InstructionList, TransactionHeader, TransactionManifest } from ".";
export declare class TransactionIntent {
    header: TransactionHeader;
    manifest: TransactionManifest;
    constructor(header: TransactionHeader, manifest: TransactionManifest);
    compile(): Promise<Uint8Array>;
    static decompile(compiledIntent: Uint8Array, instructionsOutputKind?: InstructionList.Kind): Promise<TransactionIntent>;
    transactionId(): Promise<Uint8Array>;
    toString(): string;
    toObject(): Record<string, any>;
}
