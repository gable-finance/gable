import { InstructionList } from ".";
export declare class TransactionManifest {
    instructions: InstructionList.InstructionList;
    blobs: Array<Uint8Array>;
    constructor(instructions: InstructionList.InstructionList, blobs?: Array<Uint8Array | string>);
    convert(instructionFormat: InstructionList.Kind, networkId: number): Promise<TransactionManifest>;
    toString(): string;
    toObject(): Record<string, any>;
}
