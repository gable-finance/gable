import { PublicKey } from "../crypto";
/**
 * A transaction header containing metadata and other transaction information.
 */
export declare class TransactionHeader {
    networkId: number;
    startEpochInclusive: number;
    endEpochExclusive: number;
    nonce: number;
    notaryPublicKey: PublicKey.PublicKey;
    notaryIsSignatory: boolean;
    tipPercentage: number;
    constructor(networkId: number, startEpochInclusive: number, endEpochExclusive: number, nonce: number, notaryPublicKey: PublicKey.PublicKey, notaryIsSignatory: boolean, tipPercentage: number);
    toString(): string;
    toObject(): Record<string, any>;
}
