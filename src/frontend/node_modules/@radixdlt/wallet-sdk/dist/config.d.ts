import { LogLevelDesc } from 'loglevel';
export declare const config: {
    extensionDetectionTime: number;
    defaultNumberOfAccountsQuantity: number;
    defaultNumberOfAccountsQuantifier: "exactly" | "atLeast";
    logLevel: LogLevelDesc;
};
