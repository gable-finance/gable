import { RequestItem } from '@radixdlt/connect-button';
import { BehaviorSubject, Subject } from 'rxjs';
export type RequestItemSubjects = ReturnType<typeof RequestItemSubjects>;
export declare const RequestItemSubjects: () => {
    onChange: Subject<void>;
    items: BehaviorSubject<RequestItem[]>;
    pendingItems: BehaviorSubject<boolean>;
};
