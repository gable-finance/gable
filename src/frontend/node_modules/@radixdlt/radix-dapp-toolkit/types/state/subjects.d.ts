import { ReplaySubject, Subject } from 'rxjs';
import { State } from '../_types';
export type StateSubjects = ReturnType<typeof StateSubjects>;
export declare const StateSubjects: () => {
    connected: ReplaySubject<boolean>;
    accounts: ReplaySubject<{
        address: string;
        label: string;
        appearanceId: number;
    }[] | undefined>;
    persona: ReplaySubject<{
        identityAddress: string;
        label: string;
    } | undefined>;
    onInit: ReplaySubject<State>;
    state$: ReplaySubject<State>;
    setState: Subject<{
        state: Partial<State>;
        persist: boolean;
    }>;
    updateSharedData: Subject<void>;
};
