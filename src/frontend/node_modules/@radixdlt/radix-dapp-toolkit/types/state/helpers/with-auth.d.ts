import { DataRequestValue, State } from '../../_types';
import { Result } from 'neverthrow';
export declare const withAuth: (request: DataRequestValue, persona: State['persona']) => Result<DataRequestValue, never>;
